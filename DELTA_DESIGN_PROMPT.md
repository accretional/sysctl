hold on, let's actually implement Subscribe in a way that returns MetricDeltas, represented as string metric, bytes value. Also leave a note that we want to contiunue the optimization by creating a darwin subpackage of the sysctl proto where we encode Categories and the Names within them as enums, and then later the api's responses (and optionally requests) as enums to save space in the response. In that same vein, we'll change Metric to MetricDescriptor, and have it encode type instead of value, then get rid of MetricInfo - you'll need to do an audit and see if anything actually needs variable-length responses from the kernel (I suspect not) and if so remove bytes as a valid response. The reason is that this will allow us to compress all stream responses to messages containing just one int for the metric id (Darwin.Name, which could in theory be defined as the the Darwin int used by the actual sysctl syscall) and one per value (assuming I correctly assume they don't really have variable length responses and are probably all int), only when changed. Call it DELTA_DESIGN.md and include my raw request for the note contents as DELTA_DESIGN_PROMPT.md
